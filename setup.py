from setuptools import setup, find_packages

setup(
   name='law-marketing-optimisation',
   version='1.0',
   description='Optimisation Use Case',
   author='Yi Theng',
   packages=find_packages(),  #same as name
)